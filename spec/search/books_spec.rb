require "spec_helper"

RSpec.describe Search::Books do
  it "has a version number" do
    expect(Search::Books::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
